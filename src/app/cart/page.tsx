import Container from '@/components/Container';
import Title from '@/components/Title';

const Page = () => {
  return (
    <Container>
      <Title title={'Your Cart'} />
    </Container>
  );
};

export default Page;
